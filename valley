-- Valley Prison JXL Hub 2025 - Advanced script with auto update and bypass
-- UI: Rayfield, all features included: Fly, SilentAim, KillAura, ESP, Noclip, Auto Weapon Equip, etc.

local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/shlexware/Rayfield/main/source'))()

local Window = Rayfield:CreateWindow({
    Name = "Valley Prison JXL Hub | 2025 Edition",
    LoadingTitle = "Loading JXL Thea Valley Prison Hub...",
    LoadingSubtitle = "by YEYEYEYE1231",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "ValleyPrisonJXL", 
        FileName = "Config"
    },
    Discord = {
        Enabled = false,
    }
})

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Camera = workspace.CurrentCamera

-- Utility functions for bypassed Fly and Noclip --
local FlyEnabled = false
local FlySpeed = 100
local BodyVelocity
local BodyGyro

local function FlyStart()
    if FlyEnabled then return end
    FlyEnabled = true

    local character = LocalPlayer.Character
    if not character then return end
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    BodyVelocity = Instance.new("BodyVelocity")
    BodyVelocity.Velocity = Vector3.new(0,0,0)
    BodyVelocity.MaxForce = Vector3.new(1e5,1e5,1e5)
    BodyVelocity.Parent = hrp

    BodyGyro = Instance.new("BodyGyro")
    BodyGyro.MaxTorque = Vector3.new(1e5,1e5,1e5)
    BodyGyro.CFrame = hrp.CFrame
    BodyGyro.Parent = hrp

    RunService:BindToRenderStep("FlyStep", 301, function()
        if not FlyEnabled then
            BodyVelocity:Destroy()
            BodyGyro:Destroy()
            RunService:UnbindFromRenderStep("FlyStep")
            return
        end

        local direction = Vector3.new(0,0,0)
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then direction = direction + Camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then direction = direction - Camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then direction = direction - Camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then direction = direction + Camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then direction = direction + Vector3.new(0,1,0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then direction = direction - Vector3.new(0,1,0) end

        BodyVelocity.Velocity = direction.Unit * FlySpeed
        BodyGyro.CFrame = Camera.CFrame
    end)
end

local function FlyStop()
    FlyEnabled = false
end

-- Silent Aim Core
local SilentAimEnabled = false
local SilentAimFOV = 30
local SilentAimTarget

-- ESP Core
local ESPEnabled = false
local ESPObjects = {}

local function CreateESPForPlayer(plr)
    if ESPObjects[plr] then return end
    local espBox = Drawing.new("Square")
    espBox.Visible = false
    espBox.Color = Color3.new(1,0,0)
    espBox.Thickness = 2
    espBox.Transparency = 1

    ESPObjects[plr] = espBox
end

local function RemoveESPForPlayer(plr)
    if ESPObjects[plr] then
        ESPObjects[plr]:Remove()
        ESPObjects[plr] = nil
    end
end

-- Weapon Auto Equip & Give (patched for 2025)
local function GiveAllWeapons()
    local plr = LocalPlayer
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local Remote = ReplicatedStorage:FindFirstChild("WeaponRemote") or ReplicatedS
